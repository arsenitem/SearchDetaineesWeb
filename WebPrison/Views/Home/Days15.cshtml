@model List<string[]>
@foreach (var res in Model)
{
    <tr>
        <th scope="col">@res[0]</th>
        <th scope="col">@res[0]</th>
        <th scope="col">@res[0]</th>
        <th scope="col">@res[0]</th>

    </tr>
}
